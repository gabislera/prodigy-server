import { events } from "./events";
import { notes } from "./notes";
import { taskColumns, taskGroups, tasks } from "./tasks";

export const schema = {
	notes,
	events,
	tasks,
	taskGroups,
	taskColumns,
};
